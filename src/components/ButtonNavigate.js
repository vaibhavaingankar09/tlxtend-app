import React from 'react';
import { Button } from 'react-bootstrap';

const ButtonNavigate = (props) => {
    return (<Button variant="primary">View Photos</Button>);
}

export default ButtonNavigate;